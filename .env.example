# Railway 환경 변수 설정 예시
# Railway 대시보드에서 다음 변수들을 설정하세요:

# 클라이언트 URL (프론트엔드 배포 URL) - 필수
CLIENT_URL=https://your-frontend-app.vercel.app

# 데이터베이스 URL (MySQL 서비스 추가 시 자동 생성)
DATABASE_URL=mysql://username:password@host:port/database

# 환경 설정
NODE_ENV=production

# 포트 (Railway에서 자동으로 설정되므로 보통 설정할 필요 없음)
# PORT=4000

# Socket.IO 관련 (선택사항)
SOCKET_IO_ORIGINS=https://your-frontend-app.vercel.app
# NODE_ENV=production
